namespace TestProject.Common
{
    public class Enums
    {
        public enum Role
        {
            Admin,
            Member
        }
        public enum Transaction
        {
            Charge,
            Purchase
        }
    }
}
